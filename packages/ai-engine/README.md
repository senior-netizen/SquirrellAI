# Prototype AI engine helpers

This package contains prototype helper logic that is separate from the canonical production worker.

- The deployable AI engine lives in `services/ai-engine`.
- This package is intentionally renamed to avoid colliding with the production workspace package name.
- Treat this package as experimental unless it is explicitly promoted into the production path.
