# AI Engine prototype

This FastAPI implementation is retained as a prototype reference only.

- It is **not** the authoritative production AI engine.
- The canonical production worker lives in `services/ai-engine`.
- Do not point deployment automation or operator runbooks at this directory.
