# Core platform prototype

This NestJS application is an earlier scaffold retained for reference.

- It is **not** the canonical production API gateway.
- The authoritative control-plane ingress lives in `services/api-gateway`.
- Do not point deployment automation or operator runbooks at this directory.
